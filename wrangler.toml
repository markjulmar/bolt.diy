#:schema node_modules/wrangler/config-schema.json

# The following Cloudflare Pages settings are commented out to deploy to Azure Web Apps deployment.
# These settings are preserved for reference but will not be used in the Azure deployment process.
#name = "bolt"
#compatibility_flags = ["nodejs_compat"]
#compatibility_date = "2024-07-01"
#pages_build_output_dir = "./build/client"
#send_metrics = false
